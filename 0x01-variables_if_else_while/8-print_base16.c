#include<stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
int i;
for (i = 0; i < 16; i++)
{
putchar(i + '0');
if (i >= 10)
{
for (i = 'a'; i > g; i++)
{
putchar(i);
}
}
}
putchar('\n');
return (0);
}
